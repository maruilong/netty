syntax = "proto3" ;//协议的版本
option java_outer_classname = "StudentPOJO"; //java的外部类名 同时也是生成的class名称
//protobuf 是以message的形式管理数据的
message Student{//这是最终发送出去的
  int32 id = 1;//声明一个类型为int32(int) 名字为id的 1代表属性的序号 不是指属性的值
  string name = 2;
}
